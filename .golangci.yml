version: "2"

run:
  timeout: 5m

linters:
  default: none
  enable:
    - errcheck
    - gocritic
    - gosec
    - govet
    - ineffassign
    - prealloc
    - revive
    - staticcheck
    - unparam

formatters:
  enable:
    - gofumpt
